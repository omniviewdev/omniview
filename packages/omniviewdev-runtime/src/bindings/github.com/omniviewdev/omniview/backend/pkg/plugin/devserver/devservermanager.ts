// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * DevServerManager manages dev server instances for plugins in development mode.
 * It is exposed as a Wails binding. All public methods are callable from the frontend.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * GetDevServerLogs returns the most recent log entries for a plugin's dev server.
 */
export function GetDevServerLogs(pluginID: string, count: number): $CancellablePromise<$models.LogEntry[]> {
    return $Call.ByID(3482619311, pluginID, count).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * GetDevServerState returns the current state of the dev server for a single plugin.
 */
export function GetDevServerState(pluginID: string): $CancellablePromise<$models.DevServerState> {
    return $Call.ByID(1772375401, pluginID).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * GetExternalPluginInfo returns the DevInfo for an externally-managed plugin, or nil.
 */
export function GetExternalPluginInfo(pluginID: string): $CancellablePromise<$models.DevInfoFile | null> {
    return $Call.ByID(3880526136, pluginID).then(($result: any) => {
        return $$createType4($result);
    });
}

/**
 * IsManaged returns true if the plugin's dev server is currently managed by this manager,
 * either via IDE-managed mode or external mode.
 */
export function IsManaged(pluginID: string): $CancellablePromise<boolean> {
    return $Call.ByID(1454770445, pluginID);
}

/**
 * ListDevServerStates returns the state of all running dev server instances,
 * including externally-managed plugins.
 */
export function ListDevServerStates(): $CancellablePromise<$models.DevServerState[]> {
    return $Call.ByID(1293429178).then(($result: any) => {
        return $$createType5($result);
    });
}

/**
 * RebuildPlugin triggers a Go rebuild, transfers the binary, and reloads the plugin
 * without restarting the Vite dev server.
 */
export function RebuildPlugin(pluginID: string): $CancellablePromise<void> {
    return $Call.ByID(132385600, pluginID);
}

/**
 * RestartDevServer stops and then starts the dev server for the given plugin.
 */
export function RestartDevServer(pluginID: string): $CancellablePromise<$models.DevServerState> {
    return $Call.ByID(2717676161, pluginID).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * StartDevServer starts a managed dev server for the given plugin. This spawns
 * a Vite dev server process and a Go file watcher. The plugin must already be
 * loaded in dev mode (DevMode=true, DevPath set).
 * 
 * Returns the initial DevServerState.
 */
export function StartDevServer(pluginID: string): $CancellablePromise<$models.DevServerState> {
    return $Call.ByID(1762760460, pluginID).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * StartDevServerForPath starts a managed dev server for a plugin using the
 * provided devPath directly, without requiring the plugin to be loaded first.
 * This is used during InstallInDevMode and Initialize where the plugin hasn't
 * been loaded yet but we need the dev server (and its initial Go build) to run
 * before LoadPlugin.
 */
export function StartDevServerForPath(pluginID: string, devPath: string): $CancellablePromise<$models.DevServerState> {
    return $Call.ByID(3077855780, pluginID, devPath).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * StopDevServer stops the managed dev server for the given plugin.
 */
export function StopDevServer(pluginID: string): $CancellablePromise<void> {
    return $Call.ByID(3047003960, pluginID);
}

// Private type creation functions
const $$createType0 = $models.LogEntry.createFrom;
const $$createType1 = $Create.Array($$createType0);
const $$createType2 = $models.DevServerState.createFrom;
const $$createType3 = $models.DevInfoFile.createFrom;
const $$createType4 = $Create.Nullable($$createType3);
const $$createType5 = $Create.Array($$createType2);
