// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * NetworkerControllerService wraps the networker.Controller interface.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as types$0 from "./backend/pkg/plugin/types/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as config$0 from "../plugin-sdk/pkg/config/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as networker$0 from "../plugin-sdk/pkg/v1/networker/models.js";

/**
 * ClosePortForwardSession closes a port forward session
 */
export function ClosePortForwardSession(sessionID: string): $CancellablePromise<networker$0.PortForwardSession | null> {
    return $Call.ByID(280793032, sessionID).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * FindPortForwardSessions returns a list of port forward sessions for a plugin
 */
export function FindPortForwardSessions(pluginID: string, connectionID: string, request: networker$0.FindPortForwardSessionRequest): $CancellablePromise<(networker$0.PortForwardSession | null)[]> {
    return $Call.ByID(3429152344, pluginID, connectionID, request).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * GetPortForwardSession returns a port forward session by ID
 */
export function GetPortForwardSession(sessionID: string): $CancellablePromise<networker$0.PortForwardSession | null> {
    return $Call.ByID(2765408144, sessionID).then(($result: any) => {
        return $$createType1($result);
    });
}

/**
 * GetSupportedPortForwardTargets returns the supported targets for port forwarding
 */
export function GetSupportedPortForwardTargets(pluginID: string): $CancellablePromise<string[]> {
    return $Call.ByID(2199710208, pluginID).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * HasPlugin returns true if the plugin is registered with this controller.
 */
export function HasPlugin(pluginID: string): $CancellablePromise<boolean> {
    return $Call.ByID(1410344939, pluginID);
}

/**
 * ListAllPortForwardSessions returns all port forward sessions across all plugins
 */
export function ListAllPortForwardSessions(): $CancellablePromise<(networker$0.PortForwardSession | null)[]> {
    return $Call.ByID(92526888).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * ListPlugins returns a list of names of the plugins that are installed.
 */
export function ListPlugins(): $CancellablePromise<string[]> {
    return $Call.ByID(3677762456).then(($result: any) => {
        return $$createType3($result);
    });
}

/**
 * ListPortForwardSessions returns a list of port forward sessions for a plugin
 */
export function ListPortForwardSessions(pluginID: string, connectionID: string): $CancellablePromise<(networker$0.PortForwardSession | null)[]> {
    return $Call.ByID(521717509, pluginID, connectionID).then(($result: any) => {
        return $$createType2($result);
    });
}

/**
 * OnPluginDestroy is called when the plugin is being removed from the system. This is the last chance to
 * clean up any resources associated with the plugin before it is uninstalled.
 */
export function OnPluginDestroy(pluginID: string, meta: config$0.PluginMeta): $CancellablePromise<void> {
    return $Call.ByID(3604530288, pluginID, meta);
}

/**
 * OnPluginInit is called after the plugin is loaded in to do any pre-startup initialization.
 */
export function OnPluginInit(pluginID: string, meta: config$0.PluginMeta): $CancellablePromise<void> {
    return $Call.ByID(4109477684, pluginID, meta);
}

/**
 * OnPluginShutdown is called when the plugin is shutdown. This is the last chance to clean up any resources
 * before the IDE is closed.
 */
export function OnPluginShutdown(pluginID: string, meta: config$0.PluginMeta): $CancellablePromise<void> {
    return $Call.ByID(2794955120, pluginID, meta);
}

/**
 * OnPluginStart is called when the plugin is started.
 */
export function OnPluginStart(pluginID: string, meta: config$0.PluginMeta, backend: types$0.PluginBackend): $CancellablePromise<void> {
    return $Call.ByID(165692446, pluginID, meta, backend);
}

/**
 * OnPluginStop is called when the plugin is stopped. Modules should perform any persisting of state here
 */
export function OnPluginStop(pluginID: string, meta: config$0.PluginMeta): $CancellablePromise<void> {
    return $Call.ByID(925044774, pluginID, meta);
}

/**
 * StartResourcePortForwardingSession starts a port forwarding session
 */
export function StartResourcePortForwardingSession(pluginID: string, connectionID: string, opts: networker$0.PortForwardSessionOptions): $CancellablePromise<networker$0.PortForwardSession | null> {
    return $Call.ByID(4289977092, pluginID, connectionID, opts).then(($result: any) => {
        return $$createType1($result);
    });
}

// Private type creation functions
const $$createType0 = networker$0.PortForwardSession.createFrom;
const $$createType1 = $Create.Nullable($$createType0);
const $$createType2 = $Create.Array($$createType1);
const $$createType3 = $Create.Array($Create.Any);
