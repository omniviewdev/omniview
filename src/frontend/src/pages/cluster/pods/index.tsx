// material-ui
// import { Stack } from '@mui/material';

// custom
import Table from './Table';


/**
 * Main view for all of the pods in a cluster
 * (this is just the pods table right now because we only have a table view)
 */
export default function Pods() {
  return (
    <Table />
  );
}


