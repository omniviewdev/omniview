import { create } from "@storybook/theming/create";

export default create({
  base: "dark",
  brandTitle: "Omniview",
  brandUrl: "https://docs.omniview.dev",
  brandImage:
    "https://raw.githubusercontent.com/omniviewdev/omniview/main/img/logo.svg",
  brandTarget: "_self",
});
