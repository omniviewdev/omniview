/** Props passed to dashboard widget extension components. */
export type DashboardWidgetProps = {
  pluginID: string;
  connectionID: string;
};

/** Props passed to dashboard tab extension components. */
export type DashboardTabProps = {
  pluginID: string;
  connectionID: string;
};
